export enum RCSRoutsEnum {
  OVERVIEW = "/overview",
  SCREENSHOT = "/screenshot",
  KEY_LOGGER = "/keylogger",
  VOICE_RECORDER = "/microphone",
  LOGIN = "/login",
  VERIFY = "/twostepverification",
}
